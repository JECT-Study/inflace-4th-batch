spring:
  application:
    name: inflace-batch
  datasource:
    url: jdbc:postgresql://localhost:15500/inflace?reWriteBatchedInserts=true
    username: inflace
    password: password
  batch:
    jdbc:
      initialize-schema: always
